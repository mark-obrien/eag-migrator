SQLAlchemy>=2.0.30,<3
PyMySQL>=1.1.1
# Required by PyMySQL for MySQL 8's default caching_sha2_password auth.
cryptography>=42.0
psycopg[binary]>=3.1.18
PyYAML>=6.0.1
pydantic>=2.7,<3
typer>=0.12.3
rich>=13.7
httpx>=0.27
tenacity>=8.3
python-dotenv>=1.0.1
# Web source: reading the v2 site over HTTP when there is no DB/API access.
beautifulsoup4>=4.12
lxml>=5.2
# `eagm capture` only. The pip package is small; the browser binary is not
# installed unless the image is built with --build-arg WITH_BROWSER=true.
playwright>=1.44
